@* future plans *@

@* @model GymPortal.Web.ViewModels.SetPasswordViewModel
@{
    ViewData["Title"] = "Set Your Password";
}

<div class="auth-page">
    <div class="auth-card">
        <h1 class="auth-title">Set Your Password</h1>
        <p class="auth-description">Set a strong password to protect your account. For security reasons, your password must be at least 8 characters long and include uppercase and lowercase letters, numbers, and special characters.</p>
        <hr class="auth-divider" />
        @await Html.PartialAsync("_ValidationSummary")
        <form method="post" asp-action="SetPassword" asp-controller="Account" class="auth-form" novalidate>
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Token" />

            <div class="auth-form-group">
                <label class="auth-label">Email Address</label>
                <input class="auth-input" type="email" value="@ViewBag.Email" placeholder="username@domain.com" readonly />
            </div>

            <div class="auth-form-group">
                <label asp-for="NewPassword" class="auth-label">Password</label>
                <div class="auth-password-wrapper">
                    <input asp-for="NewPassword" class="auth-input" type="password" id="newPassword" autocomplete="new-password" placeholder="Enter Password" required />
                    <button type="button" class="auth-password-toggle" onclick="togglePassword('newPassword')" aria-label="Toggle password">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                            <circle cx="12" cy="12" r="3" />
                        </svg>
                    </button>
                </div>
                <span asp-validation-for="NewPassword" style="font-size:12px;color:#B91C1C;margin-top:4px"></span>
            </div>

            <div class="auth-form-group">
                <label asp-for="ConfirmNewPassword" class="auth-label">Confirm Password</label>
                <div class="auth-password-wrapper">
                    <input asp-for="ConfirmNewPassword" class="auth-input" type="password" id="confirmPassword" autocomplete="new-password" placeholder="Confirm Password" required />
                    <button type="button" class="auth-password-toggle" onclick="togglePassword('confirmPassword')" aria-label="Toggle password">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                            <circle cx="12" cy="12" r="3" />
                        </svg>
                    </button>
                </div>
                <span asp-validation-for="ConfirmNewPassword" style="font-size:12px;color:#B91C1C;margin-top:4px"></span>
            </div>

            <button type="submit" class="auth-submit">
                Set Password
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                    <path d="M5 12h14M12 5l7 7-7 7" />
                </svg>
            </button>

            <label class="auth-checkbox-label" style="justify-content:center;margin-top:16px">
                <input type="checkbox" required />
                <span class="auth-checkbox-custom"></span>
                I accept the <a href="#" class="auth-checkbox-link">terms and conditions</a>
            </label>
        </form>

        <p class="auth-footer">
            <a href="/Account/SignIn" class="auth-link">← Back to Sign In</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function togglePassword(id) {
            var input = document.getElementById(id);
            input.type = input.type === 'password' ? 'text' : 'password';
        }
    </script>
}
 *@